// Admin is an internal tool — re-export from the root admin page
// The locale layout wraps it, so the language switcher is available via the nav if needed
export { default } from '../../admin/page';
